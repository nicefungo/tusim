# Realistic-Design Implementation Backlog

**Last audit:** 2026-09-02
**Scope:** Evidence-backed pre-spec alternatives; `PRODUCTION_TU_REDESIGN.md` is reference only.

## Status legend

- **READY:** reproducible evidence, valid model path, actionable tests
- **BLOCKED:** valuable but a dependency/model gap prevents honest implementation
- **DONE:** implemented, tested, and documented
- **EXCLUDED:** strong documented reason not to implement

## Candidates

| Priority | Candidate / alternatives | Hardware rationale and evidence | Gains and sacrifices | Observable now | State / dependency / verification |
|---|---|---|---|---|---|
| P0 | Weight stream: NONE / exact RLE / epsilon RLE | `weight-compression-rle-sweep.md`; real designs may omit a codec for fixed-rate simplicity or add RLE for clustered/block-pruned weights | RLE ranges from 3.0× traffic expansion to 585× reduction in measured patterns; decoder area/power/backpressure and epsilon accuracy costs remain unquantified | Encoded bytes, runs, payload DMA cycles, round-trip correctness, config parse | **DONE 2026-07-17.** Canonical runtime config, validation, portable 6-byte wire format, 13 tests, sweep target. Default NONE. |
| P0 | Per-tensor adaptive raw/RLE selection | `weight-compression-rle-sweep.md`: random 50–70% sparsity expands bare RLE traffic while clustered tensors compress strongly | Bounded fallback avoids expansion beyond a 16-byte frame; costs version/codec metadata, raw bypass, two decode paths, and compiler/runtime format state | Encoded size, selected codec, payload cycles, exact round-trip, corrupt-frame rejection | **DONE 2026-07-18.** Runtime `adaptive_rle`, explicit versioned frame, RAW/RLE selection, DMA/config integration, 17 focused tests. Default remains NONE. |
| P0 | Structured sparse MMA: dense / 2:4, decoder 1 / 4 / 16 groups per cycle | `structured-2of4-sweep.md`; current checkout contained a dormant functional module but it was absent from the build, runtime mode was not validated, and “2x” ignored decode/DMA costs | FP16 weight bytes fall 37.5%; measured latency ranges from 0.444x to 1.834x dense depending on shape/decoder. Costs: pruning accuracy, decoder/steering area and power, control and compiler packing | Functional output/MACs/bytes; analytical DMA/compute/decode/total cycles; config parse | **DONE 2026-07-20.** Dense default plus runtime 2:4 and decoder throughput; module linked into libraries; 27 focused tests and 15-row sweep. Unstructured mode and malformed configs are rejected. ISA auto-dispatch and physical decoder costs remain unmodeled. |
| P1 | Context switching: FULL_SRAM / LIVE_SRAM / CONTROL_ONLY; no preemption / RR / priority | `context-switch-state-scope.md`; physically plausible retention ranges from dedicated full context store to compiler-managed reload | Full preserves isolation but costs 2× retained bytes/switch; live reduces traffic/storage but requires liveness metadata; control minimizes hardware state but forces SRAM reloads and loses transparent isolation | Retained bytes, save+restore cycles, fixed pipeline cost, functional prefix/full restoration, scheduler behavior | **DONE 2026-07-19.** Runtime scope, per-region live prefixes, transfer BW, validation, 15 focused tests, and executable 3-size/3-scope sweep. FULL with BW=0 preserves legacy fixed-only timing/default semantics. |
| P1 | Interconnect topology: NONE / RING / MESH | Historical `interconnect-topology-sweep.md` used implicit store-forward payload×hop timing; `interconnect-contention-traffic-matrix.md` now measures deterministic routes and shared directed links | At 16 cores MESH lowers all-to-all bound 55.4%, while RING lowers hotspot fan-in 32.7%; mesh costs links/router ports, ring sacrifices distributed bandwidth | Route distance, switching-aware transfer cycles, bottleneck directed-link service, simultaneous traffic lower bound | **DONE for deterministic shared-link lower bound 2026-07-24; BLOCKED for final recommendation.** Runtime topologies retained. Queue order/backpressure, adaptive routing, arbitration, and collective algorithms remain dependencies. |
| P0 | Interconnect switching: legacy hop-only / cut-through / store-and-forward | `interconnect-switching-modes.md`; real NoCs trade flit-pipelined latency/control against simpler packet-level forwarding and buffering | At 16 B/cycle, cut-through is equal at 1 hop and 1.42–6.95× lower latency over measured 3–7-hop cases; costs VC/credit/deadlock complexity, while store-forward costs packet buffers and repeated buffer traffic | Route hops, payload/link serialization, cycles, config parse, byte-exact sends | **DONE 2026-07-23.** Runtime modes, link width, router latency, shared estimator, send accounting, 14 multicore tests, 20 config tests, and 12-row sweep. Legacy default preserves behavior. Contention/queues/router area-power remain unmodeled. |
| P0 | Interconnect contention fidelity: IDEAL_PARALLEL / SHARED_LINK lower bound | `interconnect-contention-traffic-matrix.md`; simultaneous fan-in and all-to-all expose bottlenecks hidden by isolated transfers | Shared links preserve finite bandwidth and workload-dependent topology trade-offs; ideal mode preserves a useful lower bound. Shared-link mode costs route accounting and still understates queues/HOL stalls | Ideal/shared cycles, directed-link serialization, bottleneck endpoints, config parse | **DONE 2026-07-24.** Runtime mode, RING shortest-path and MESH XY route accounting, public traffic-matrix API, 15 multicore tests, 20 config tests, and 12-row sweep. Ideal default preserves behavior. |
| P0 | Deterministic mesh routing: XY / YX | `interconnect-mesh-routing-order.md`; both fixed dimension orders are minimal and physically simple, but asymmetric placement can concentrate traffic on different links | Measured 4×4 asymmetric fan-in improves 65.8% with the matching order and reverses exactly for the transposed traffic; runtime selection adds control state while adaptive routing remains unmodeled | Per-route directed-link service, total lower-bound cycles, bottleneck endpoints, config parse | **DONE 2026-07-25.** Runtime/canonical/YAML/JSON mode, validation, deterministic path selection, 16 multicore tests, 20 config tests, and 12-row sweep. XY default preserves prior behavior. |
| P0 | Dataflow integration and evidence: WS / OS / RS | `dataflow-plugin-executable-reaudit.md`; physically plausible stationarity choices were present, but the historical core sweep could execute a different active plugin and its formulas diverged from the live K-tile dispatcher | Runtime selection and depth are now effective; measured live estimates vary by mode/depth, but physical throughput, traffic, area, and energy rankings remain uncalibrated | Active plugin, canonical raw-bit oracle, valid-edge/multi-K tile counts, live MMA cycle delta, config parse/propagation | **DONE for direct-global functional/integration evidence 2026-07-26; per-core isolation 2026-07-27.** 21-row fail-closed global sweep plus core-local selection/isolation gates. Physical movement/traffic remains BLOCKED on explicit event and overlap contracts. |
| P1 | Core dataflow scope: homogeneous fixed / global selectable / per-core WS-OS-RS | `per-core-dataflow-selection.md`; symmetric fixed cores minimize hardware and software complexity, while independent modes can serve mixed kernel phases or represent physically heterogeneous cores | Independent mode retention enables heterogeneous mapping studies; costs per-core state, selectable movement/control, scheduling and verification. Switch latency, physical mux/area, and parallel makespan remain unquantified | Active core mode, isolated core cycle delta, byte-identical outputs, unsupported-mode rejection | **DONE 2026-07-27.** Core-local set/get/name API, fail-closed global/core setters, 17-test multicore suite, and 4-workload static sweep. Built-in plugin statistics and `g_tu` swap execution remain process-global, so concurrent host threads and per-core plugin counters are unsupported. |
| P0 | SRAM per-bank provisioning: compatibility 1 grant/4 cycles / single issue / dual issue / wide issue | `sram-issue-grant-cadence.md`; the live refill budget made the historical “words per cycle” label false when the window exceeded one, and canonical knobs were not propagated | In exact four-request patterns, 1/1 removes the compatibility throttle for spaced requests; same-cycle stalls fall 6→4→0 for 1/2/4 grants. Wider issue costs expected ports/subbanks, area, routing, and energy; physical costs remain unquantified | Runtime bank count, grant/window/penalty, exact per-bank stalls, config parse/propagation | **DONE 2026-08-22.** YAML/JSON/generator/canonical/runtime path, zero-init defaults, exact five-row sweep, and invalid-config gates. Top-level word width remains fixed at 4 B because scalar engine accesses lack size metadata; scalar calls do not replay stalls or prove physical ports. |
| P0 | DMA channels / outstanding depth: 1 / 2 / 3 canonical streams, model capacity 8; 1 / 2 / 4 descriptors | `dma-channel-queue-sweep.md` live re-audit; W/A/O streams can share one simple engine or use independent engines, while descriptor storage controls admitted lookahead | For two equal 4 KiB streams, live completion is 4,325 ticks on one channel versus 2,163 on two or three; for three streams it is 6,487 / 4,325 / 2,163 on 1/2/3 channels. More channels cost engines, ports, routing, CDC/control, and verification; deeper queues cost descriptor storage and bookkeeping but this model has no producer-latency throughput term | Byte-exact movement, channel completion ticks, accepted descriptors, active+queued outstanding count, JSON/canonical/runtime propagation | **DONE 2026-08-23.** Runtime config now reaches the live DMA engine; fixed 8-entry model capacity is independent of selected count; unsupported counts fail closed; max-outstanding counts active plus pending work. 12 DMA tests, 31 config tests, and a 12-row fail-closed sweep. Shared DRAM/SRAM port contention, queue producer timing, area/power, and GEMM overlap remain unmodeled; topology, arbitration, and binding are covered separately below. |
| P0 | DMA bus width: 32–1024-bit power-of-two range; representative 128 / 256 / 512 bit | `dma-runtime-bus-width.md`; the width was parsed and validated but dropped before runtime, while narrow movers reduce expected ports/routing and wide movers can feed bandwidth-heavy arrays | Measured isolated 4 KiB completion is 307 / 179 / 115 cycles at 128 / 256 / 512 bit. Wider paths trade lower serialization latency for expected datapath, FIFO, SRAM-port, routing, switching, timing, and verification cost; physical costs are unquantified | Parse-to-live width, exact descriptor completion/payload cycles, byte movement, default/zero compatibility, invalid rejection | **DONE 2026-08-29 for live descriptor service.** Runtime width now reaches live and queued descriptor estimates through an architecture-aware initializer; old APIs and zero runtime fields retain 256-bit compatibility. Exact 3-row sweep plus DMA/config/full-build gates. Compile-time-only compute-engine estimators, physical burst/alignment, shared memory limits, area/power, and calibration remain separate/unmodeled. |
| P0 | DMA directional base latency: symmetric / read-fast / write-fast × core cycles / physical ns | `dma-directional-base-latency.md`; configured read/write latency was dropped before runtime and every descriptor direction paid the read constant, preventing inference-load versus result-drain trade-offs | At 4 KiB/256 bit, 50/50 completes load/store at 179/179 cycles; 30/70 gives 159/199 and 70/30 reverses to 199/159. Asymmetry can prioritize operand fetch or output drain but may require unequal buffers/pipeline resources; physical cost is unquantified | Parse/domain/clock conversion, live directional cycles, queued projected estimate input, exact bytes, default/zero compatibility | **DONE 2026-08-30.** Independent runtime read/write values, cycle/ns conversion, explicit-valid compatibility bit, additive timing initializer, direction-aware live/legacy/projected service, and exact three-row sweep. DRAM queues, command/data phasing, write combining, shared bandwidth, area/power, and calibration remain unmodeled. |
| P0 | DMA burst command rate: 16–65536 B max burst × 0–1024 issue cycles; representative 32 / 64 / 128 B × zero / one-cycle issue | `dma-burst-issue-cost.md`; advertised max burst was parsed but dropped, and maximum size alone cannot change service without an explicit issue/command cost | For a 4 KiB/256-bit load, zero-cost 64 B remains 179 cycles; one issue cycle gives 307/243/211 cycles at 32/64/128 B. Larger bursts amortize commands but are expected to cost buffering/credit scope and can increase blocking/alignment burden; physical costs are unquantified | Parse-to-live burst/cost, abstract burst count, live and queued service, legacy accounting, exact bytes, compatibility/default/rejection | **DONE 2026-08-31.** Runtime max burst and explicit per-burst issue cost now reach descriptor service and projected binding. Zero issue cost preserves historical behavior; old APIs retain 64 B/zero cost. Four-row exact sweep plus DMA/config/build gates. Functional movement remains one aggregate copy; alignment, protocol boundaries, command/data overlap, finite credits, direction-specific limits, physical area/power, and calibration remain unmodeled. |
| P0 | DMA direction-specific burst limits: symmetric / read-wide / write-wide | `dma-directional-burst-limits.md`; one common burst limit cannot represent long operand fetches with fine result writes or fine gathers with coalesced output stores | For 96 B at 256 bit with two issue cycles/burst, 128/32 B read/write changes load/store completion from 58/58 to 56/60 cycles; 32/128 reverses to 60/56. Asymmetry trades directional command pressure and expected buffer/credit resources; physical costs are unquantified | Parse-to-live direction limits, inheritance, burst count, live/queued/legacy cycles, exact bytes, rejection | **DONE 2026-09-01.** Common 64 B compatibility plus zero-inherits-common directional overrides, additive initializer, live and projected direction selection, exact six-transfer sweep, DMA/config/build gates. Address boundaries, masks/coalescing, finite credits, physical area/power, and calibration remain unmodeled; directional issue cost is covered separately below. |
| P0 | DMA direction-specific burst-issue cost: symmetric / read-overlapped / write-overlapped | `dma-directional-issue-cost.md`; separate command pipelines, prefetch, write combining, or unequal CDC/protocol control can expose different issue costs even with identical burst geometry | At 96 B with three 32 B bursts, 0/4-cycle read/write issue changes load/store completion from 60/60 to 54/66 cycles; 4/0 reverses to 66/54. Overlap can lower directional latency but is expected to require queue/credit resources and control activity; physical costs are unquantified | Parse-to-live values and presence bits, explicit zero, live/queued/legacy/direct cycles, exact bytes, rejection | **DONE 2026-09-02.** Common compatibility cost plus optional explicit directional overrides; presence metadata distinguishes zero from inheritance. Exact six-transfer sweep plus DMA/config/generator/build gates. Command FIFO depth, explicit command/data overlap, finite credits, shared-memory throughput, physical area/power, and calibration remain unmodeled. |
| P0 | DMA data-path topology: independent per-channel / shared serial bus | `dma-channel-queue-sweep.md` 2026-08-24 follow-up; high-throughput designs may replicate W/A/O movers, while cost-constrained designs retain separate queues behind one data path | Two ready streams complete in 2,163 ticks independent versus 4,325 shared; three complete in 2,163 versus 6,487. Independent costs expected engines/ports/routing/power; shared sacrifices concurrent payload service but preserves queue isolation | Byte-exact movement, active-transfer concurrency, completion ticks, config parse/propagation/default/rejection | **DONE 2026-08-24.** Runtime `independent` zero/default plus `shared_serial`, non-preemptive descriptor service, 18-row sweep, 12 DMA tests, and 31 config tests. Partial sharing, aggregate DRAM caps, SRAM arbitration, preemption, physical area/power, and compute overlap remain unmodeled; shared arbitration policies are covered separately below. |
| P0 | Shared-serial DMA arbitration: round-robin / strict descriptor priority | `dma-shared-arbitration.md`; fair general-purpose movers should rotate queues, while QoS/deadline designs may protect a critical stream using existing descriptor priority metadata | Strict priority lowers measured critical completion 4,325→2,163 ticks but delays the low-priority descriptor 2,163→6,487; batch completion remains 6,487. Priority costs comparator/control state and can starve low-priority work | Descriptor-boundary selection/order, exact completion ticks, byte movement, config parse/propagation/default/rejection | **DONE 2026-08-25.** Runtime/canonical/YAML/JSON `round_robin` zero/default and `strict_priority`; rotating tie-break, invalid-mode rejection, 13 DMA tests, 31 config tests, and exact two-row sweep. Aging/weighted service, preemption, producer priority assignment, area/power, and queue-aware DRAM remain unmodeled. |
| P0 | DMA descriptor channel binding: explicit / round-robin / least-outstanding / least-bytes / least-projected-cycles | `dma-channel-binding.md`; dedicated W/A/O movers favor compiler placement, homogeneous paths can rotate, and dynamic producers can inspect descriptor count, bytes, or a completion countdown plus queued service estimate | In a skewed six-descriptor independent-path arrival, round-robin/least-outstanding/projected reduce batch 1,325→613 cycles; least-bytes reaches 664 while balancing assigned bytes 12/12/8 KiB. Projected cycles uses exact elapsed active service but only base+serialization for queued work. Costs include distributor, counters/timestamps, adders/comparators, tie state, and loss of placement control | Accepted-submission assignment, active remaining cycles, coarse queued service, exact completion cycles, byte movement, config parse/propagation/default/rejection | **DONE 2026-08-26; least-bytes 2026-08-27; projected-cycles 2026-08-28.** `explicit` remains zero/default; automatic modes rotate ties and commit only after acceptance. 14 DMA tests, 31 config tests, and exact five-row sweep. Queued SRAM/backpressure prediction, affinities, shared-memory contention, path area/power, and adaptive switching remain unmodeled. |
| P0 | SRAM arbitration: NONE / RR / PRIORITY with simultaneous requesters | `sram-arbitration-sweep.md` executable re-audit; prior numbers were analytical and the harness is absent. Live `arb_mode` is stored but never consumed, and scalar calls have no requester identity | RR offers fairness; priority can protect critical reads; NONE can model independently ported traffic. Costs and gains cannot be measured without simultaneous grants/replay | Grant/refill counters only; no executable arbitration decision | **BLOCKED on a batched request/port contract.** Define requester identity, read/write port restrictions, same-cycle grant order, replay/time advance, fairness state, and exact tests before implementation. Do not cite historical priority rankings as live evidence. |
| P1 | Bitmap/RLE decoder throughput: serial / balanced / bus-width / extra-wide; overlapped / staged DMA | `weight-decoder-throughput.md`; payload-only cycles overstated gains because compressed streams must reconstruct dense FP16 weights | Narrow decoders minimize expected area but measured latency is 2–16× raw; bus-width decode breaks even; 2×-bus decode gives 1.03–2× measured gains in selected sparse regimes but costs lanes/FIFOs/SRAM bandwidth | Parsed stream metadata, DMA/decode/total cycles, bottleneck, overlap mode, config parse | **DONE 2026-07-22.** Runtime output/run/bitmap widths and overlap, stream-aware cycle API, 24 focused tests, 48-row sweep. Default payload-only behavior preserved. FIFO depth, SRAM conflicts, physical area/power remain unmodeled. |
| P0 | Power assumptions: AUTO / explicit 45-3nm process presets; AUTO / explicit clock | `power-process-clock-assumptions.md`; process and clock were inferred from PE dimensions and DRAM BW, confounding architecture comparisons. Mature and advanced nodes represent cost/risk versus density/energy targets; clock points represent margin/power versus latency | Fixed 7nm clock sweep: 750→2500 MHz cuts converted latency 3.33x and raises average power 2.54x; constant-voltage table energy falls 23.7% from shorter leakage duration. Process-table rows vary area/energy monotonically but omit NRE/yield/timing/thermal costs | Canonical parse/validation, generated constants, activity energy, latency, average power, area and breakdown | **DONE 2026-07-28.** AUTO default preserves zero-initialized callers and legacy heuristic; six explicit node presets and explicit 0-10GHz clock are runtime-configurable. 22 config tests, 20 power tests, and 9-row static sweep. Clock override is not DVFS; voltage/timing/thermal/calibration remain unmodeled. |
| P0 | DRAM row policy: compatibility legacy / open page / closed page | `dram-row-buffer-policy.md`; tensor streams may reuse rows while random, mixed-tenant, or adversarial traffic favors predictable closure | Historical equal-cost timing measured open-page service reductions of 27.7% sequential and 21.4% bank-stream, with a tie under row thrash; the split-cost follow-up below exposes the missing closed-page regime | Per-bank row hits/misses, read/write service cycles, config parse/propagation | **DONE 2026-07-29; hardened 2026-08-07; adaptive follow-up 2026-08-10.** Canonical static policies and compatibility default preserved; the separate adaptive-timeout row below adds the low-complexity middle point. Request queues/reordering, power wiring, and calibration remain unmodeled. |
| P0 | DRAM row timing: equal-cost compatibility / split activation and replacement | `dram-row-timing-split.md`; activation from a closed bank omits the precharge needed to replace a different open row, so one generic miss cost structurally prevents closed-page from winning | With activate=20/replacement=40, open-page is 27.68% lower service for sequential locality while closed-page is 21.95% lower under same-bank row thrash. Split timing adds calibration/control observability; equal cost remains compatibility | Aggregate misses/hits plus empty activations/replacements, returned service, config parse/propagation | **DONE 2026-08-07.** Runtime split timing API and canonical field, 0=inherits compatibility, generated YAML/JSON/header/docs path, 28 focused DRAM tests, and 12-row gated sweep. JEDEC command timing, queue overlap, and activate/precharge energy remain unmodeled. |
| P0 | DRAM page policy: static open / static closed / adaptive idle timeout | `dram-adaptive-row-timeout.md`; static endpoints suit strong locality or random traffic, while a per-bank timeout can retain short reuse and precharge stale rows without a history predictor | Adaptive matches open-page at 820 cycles for 2-cycle-gap reuse, matches closed-page at 1,120 for 16-cycle-gap reuse, loses to closed-page under immediate thrash, and wins the measured burst-pair regime at 960 vs 1,100/1,120 cycles. Costs per-bank age/comparator state, tuning, commands, and verification | Returned service, hits, activations, replacements, timeout closures, config parse/propagation | **DONE 2026-08-10; timeout-domain follow-up 2026-08-11.** Runtime `adaptive_timeout` plus nonzero threshold, fail-closed API/config, per-channel×bank age state, 31 DRAM tests, 29 config tests, and exact policy/domain sweeps. Lazy precharge, command timing/energy, queues, and calibration remain unmodeled. |
| P0 | DRAM adaptive-timeout domain: fixed core cycles / physical nanoseconds | `dram-row-timeout-domain.md`; synchronous controllers may use a direct cycle counter, while a physical idle policy should remain stable across core-clock choices | At an 8-unit threshold, fixed cycles represent 16/8/4 ns at 0.5/1/2 GHz and change a 6 ns reuse from hit to timeout at 2 GHz; physical ns converts to 4/8/16 cycles and preserves the decision. Physical mode costs conversion/state and transition verification | Converted threshold, returned service, hits/timeouts, config parse/propagation, clock recomputation | **DONE 2026-08-11.** Both runtime domains, compatibility wrapper/default, physical-ns source, failed-setter gates, 31 DRAM tests, 29 config tests, and 12-row sweep. Separate DRAM clock, CDC, dynamic transitions, legal PRE timing, queues, energy, and calibration remain unmodeled. |
| P0 | DRAM/TU core clock: 0.5 / 1 / 2 GHz comparison, continuous runtime setting | `dram-core-clock-cycle-domain.md`; the shipped knob was ignored, while fixed GB/s and physical refresh ns require clock-aware conversion | 64 GB/s maps to 128/64/32 B/core-cycle and compatibility-mode 4 KiB estimates to 82/114/178 cycles at 0.5/1/2 GHz. Faster clocks may raise compute rate but cost timing margin, power, clock resources, and memory feed per cycle | Transfer-estimate cycles, bandwidth-window capacity, refresh interval/lockout cycles, config parse/propagation | **DONE 2026-08-08; latency-domain follow-up 2026-08-09.** Runtime `(0,10]` GHz with 1 GHz default/zero-init compatibility, validated setter, generated config path, and focused sweep. Read/write latency now has explicit cycle/ns modes; voltage/DVFS, CDC, power/thermal, queues, and calibration remain unmodeled. |
| P0 | DRAM base-latency domain: fixed core cycles / physical nanoseconds | `dram-latency-domain.md`; controller/core pipeline budgets can be cycle-defined while external memory/bridge delay is physically timed, and the old implicit cycle term changed physical meaning across clock sweeps | At fixed 64 GB/s and source latency 50, the 4 KiB estimate is 164/114/89 ns in core-cycle mode but remains 114 ns at 0.5/1/2 GHz in physical-ns mode. Physical mode costs clock-domain state/rescaling; both aggregate terms remain uncalibrated | Executable read/write cycles, transfer estimate, source domain/value, config parse/propagation, clock-change recomputation | **DONE 2026-08-09.** Both runtime modes, compatibility default, failed-setter immutability, 30 DRAM tests, 28 config tests, generated-header gate, and 6-row sweep. Additive controller/CDC/DRAM decomposition, in-flight transitions, voltage/DVFS feasibility, queues, and calibration remain unmodeled. |
| P0 | DRAM channel-bus turnaround: NONE / symmetric / asymmetric × core cycles / physical ns | `dram-read-write-turnaround.md`; separate paths or compatibility studies may omit a cost, while a shared bidirectional bus needs directional guard time and real interfaces can have unequal R→W/W→R constraints | Alternating asymmetric 3/8-cycle traffic costs 108 vs 72 cycles (+50.0%); read-first batching reduces it to 75 (-30.6% vs alternating), while reverse batching costs 80. Costs: per-channel direction state, timing registers, control/verification, and scheduler complexity | Direction events/cycles, returned service, converted costs, config parse/propagation | **DONE 2026-08-12.** NONE default plus fixed symmetric/asymmetric costs and cycle/ns domains; 32 DRAM tests, 30 config tests, and 18-row exact sweep. Queueing, write-drain scheduling, fairness, command/data-bus overlap, PHY energy, and calibration remain unmodeled. |
| P0 | DRAM turnaround temporal accounting: full fixed cost / idle-time residual credit | `dram-turnaround-idle-credit.md`; a conservative model may charge every direction boundary, while a controller with a trustworthy service-completion timeline can satisfy guard time during channel idle | Back-to-back idle-credit exactly matches fixed; measured W→R service falls 26→22→18 cycles as gap grows 8→12→16. Costs: timestamp comparison/subtraction and stronger temporal-contract verification; no bytes or arithmetic change | Direction events, residual turnaround cycles, returned service, config parse/generator | **DONE 2026-08-13.** Runtime `idle_credit` preserves NONE and fixed alternatives/defaults, uses per-channel service completion, and has 12 exact zero/partial/full-credit sweep gates plus 32 DRAM and 30 config tests. Command/data-burst completion, queues, ranks, PHY timing/energy, and calibration remain unmodeled. |
| P0 | DRAM turnaround completion boundary: base-service / serialized-burst credit | `dram-turnaround-completion-boundary.md`; a lightweight model may treat aggregate base latency as completion, while a conservative shared bus must keep the channel occupied for payload serialization | For 64 B on an 8 B/cycle channel, full W→R credit shifts from gap 16 to 24 cycles; a 16 B control reaches full credit at gap 20. Costs: byte/width division and a more conservative channel timeline; command/data overlap remains unquantified | Payload-dependent completion boundary, residual turnaround, exact mode/config gates | **DONE 2026-08-14.** Runtime `burst_credit` adds `ceil(bytes/bus_width)` after base service without replacing NONE/FIXED/IDLE_CREDIT. The expanded 18-row sweep gates both directions, payload sizes, and zero/partial/full credit. JEDEC burst phasing, queues/reordering, ranks, PHY timing/energy, and calibration remain unmodeled. |
| P0 | DRAM serialized occupancy granularity: exact bytes / protocol-burst rounding | `dram-burst-granularity.md`; byte-stream fabrics may occupy only valid payload beats, while fixed-burst DRAM interfaces fetch/drive a complete transfer granule for sub-burst and tail requests | At a 64 B burst and 8 B/cycle bus, a 16 B W→R case at gap 20 changes from 0 to 4 residual cycles; an 80 B request reaches full credit at gap 26 exact versus 32 rounded. Rounding costs extra occupied beats/energy for partial requests but needs only align/shift logic for power-of-two bursts | Exact completion boundary, residual turnaround/service, config parse/generator/default | **DONE 2026-08-15.** Runtime `burst_round_credit` preserves exact-byte `burst_credit` and all simpler modes. A 28-row exact sweep gates aligned, sub-burst, multi-burst-tail, both directions, and zero/partial/full credit. Burst coalescing, masks, command/data phasing, queues, PHY energy, and calibration remain unmodeled. |
| P0 | DRAM occupied traffic: logical requested bytes / exact-byte occupancy / fixed-burst occupancy | `dram-occupied-byte-accounting.md`; logical API traffic and occupied channel traffic differ when a fixed-burst interface overfetches partial/tail requests | Measured 16 B request pairs occupy 4.0× bytes and 80 B pairs 1.6× under 64 B rounding; aligned 64 B traffic is unchanged. Fixed bursts simplify the interface contract but raise bandwidth pressure and expected dynamic energy; queues and physical energy remain unquantified | Logical and occupied read/write bytes, pending traffic, coarse bandwidth budget, completion timing | **DONE 2026-08-16.** Separate occupied-byte counters now share the live completion-rounding helper and feed pending/coarse-bandwidth accounting. NONE/FIXED/IDLE/exact-burst retain logical-byte occupancy and default behavior. The 28-row sweep and 32-test DRAM suite gate counters, reset, partial/tail/aligned traffic, and budget consumption. |
| P0 | DRAM bandwidth observability: useful payload / occupied interface | `dram-payload-efficiency.md`; fixed-burst overfetch was counted but absent from derived bandwidth/utilization, obscuring the resource cost of partial requests | With 64 B bursts, measured payload efficiency is 25.0% for 16 B, 100.0% for aligned 64 B, and 62.5% for 80 B tails. Dual reporting adds no modeled data traffic but increases metric/API and verification surface; physical energy and feasible throughput remain unquantified | Useful/occupied read/write GB/s, useful/occupied utilization, payload efficiency, exact counters and completion timing | **DONE 2026-08-17.** Historical effective BW/utilization retain useful-payload semantics; new occupied BW/utilization and payload-efficiency fields expose overfetch without changing modes/defaults. The 28-row sweep and 32-test DRAM suite gate exact ratios and read/write derived values. |
| P0 | DRAM direction-specific burst granularity: symmetric / read-wide / write-wide / exact-byte | `dram-directional-burst-granularity.md`; full-line reads with masked writes and fine reads with write combining are both plausible, while one shared granule cannot represent either asymmetry | At a 20-cycle gap, read-wide 128/32 B raises R→W service 19→21 but lowers W→R 22→18; write-wide 32/128 B lowers R→W to 18 but raises W→R to 26. Asymmetry costs direction-specific control and likely mask/merge buffers; physical area/power remain unquantified | Direction-specific completion boundary, occupied bytes, pending/bandwidth budget, payload efficiency, config parse/propagation | **DONE 2026-08-18.** Independent power-of-two read/write granules with symmetric 64 B defaults and zero-means-preset compatibility; invalid setters/configs fail closed. Eight exact sweep rows plus 32 DRAM and 30 config tests. Coalescing, masks, read-modify-write, queues, and physical energy remain unmodeled. |
| P0 | DRAM fixed-burst alignment: exact bytes / payload-size rounded / address-span rounded | `dram-burst-alignment.md`; aligned DMA or packet padding can round by payload length, while a line/burst interface with arbitrary addresses must count every aligned granule touched | A 64 B request at address 1 occupies 64 B size-rounded versus 128 B span-rounded; an 80 B request at address 63 occupies 80/128/192 B exact/size/span. Span fidelity costs address/split state and exposes traffic amplification; physical area/power remain unquantified | Address-aware occupied bytes, completion boundary, turnaround service, counters, bandwidth budget, config parse/generator | **DONE 2026-08-19.** Additive `burst_span_credit` preserves exact and size-rounded modes/defaults. Eight exact sweep rows plus 32 DRAM and 30 config tests gate aligned/misaligned/tail traffic and both directions. Multi-decode row/channel service, split/merge queues, masks, coalescing, and physical energy remain unmodeled. |
| P0 | Zero-byte DRAM request semantics: reject upstream / service-and-planning no-op / fictitious minimum burst | `dram-zero-byte-semantics.md`; empty transfers can arise after predication or shape/tail collapse, but service and planning APIs must not fabricate traffic or latency from no payload | No-op removes false latency, planner reservations, row activation, direction history, and up to one false span-rounded burst. Upstream rejection would improve diagnostics but requires status/retirement semantics; minimum-burst charging is excluded without a real issued command | Returned and estimated cycles/stall, request/occupied counters, row/direction/channel state, pending bytes across all timing modes | **DONE 2026-08-20; estimator consistency hardened 2026-08-21.** DRAM read/write and bulk estimation now return a side-effect-free zero before all ideal/non-ideal accounting. A RED→GREEN 33-test DRAM gate and 12-row mode/direction service-and-estimate sweep plus one-byte control verify boundaries. Command/ISA/DMA-level rejection remains a separate unmodeled contract. |
| P0 | DRAM address mapping: burst-interleaved / row-interleaved / XOR-interleaved | `dram-address-mapping.md`; predictable fine striping and row placement serve explicit software layouts, while XOR hashing can break power-of-two stride aliases | Row mapping cuts measured contiguous row-service 1.22% but uses 2/4 channels. For a 2 KiB stride, XOR preserves burst's 3,520 service cycles and 48 hits while changing occupancy from 64/0/0/0 to 16/16/16/16; decode delay, area/power, and queue-aware makespan remain unquantified | Exact channel/bank/row decode, row service/hits/misses, channel occupancy, config parse/propagation and geometry rejection | **DONE 2026-07-31.** Three runtime modes; burst remains zero/default. XOR requires power-of-two channels and fails closed otherwise. 18 focused DRAM tests, 22 config tests, generated-header check, and 6-row sweep. More complex hashes remain blocked on traces and queue-aware concurrency. |
| P0 | DRAM refresh: NONE / ALL_BANK / PER_BANK × FIXED / DEFERRED × 1x/2x/4x | `dram-refresh-model.md`; real DRAM must refresh every tREFI; simple controllers issue all-bank REFAB while DDR5-style controllers stagger per-bank refreshes, and scheduling may be fixed or opportunistically deferred within a retention bound | Measured per-bank refresh costs +1.0% vs +15.4% all-bank steady service at 1x (rate 4x: +61.4%); fixed vs deferred reverses with workload phase (burst-idle fixed pays 0 refresh stall, deferred +325%); refresh precharges open rows; costs per-bank control state/commands and deadline logic; refresh energy and queue-aware overlap unquantified | Returned service cycles, refresh events, per-access refresh stall, row hits/misses after precharge, config parse/propagation and rejection | **DONE 2026-08-04.** Runtime mode/scheduling/rate/timings with zero-means-defaults and fail-closed setter/validation, generated constants, canonical/JSON/YAML path, 9 focused DRAM tests (27/27), 4 config tests (26/26), and 18-row gated sweep. A follow-up phase-alignment sweep (11 phases × fixed/deferred, 22 rows) quantifies the scheduling trade-off: fixed is a phase sawtooth (0 stall on 8/11 phases; worst +81%), while deferred is phase-insensitive at +325–407% because this sub-tREFI burst pattern always contains a post-schedule access that triggers the outstanding refresh. NONE remains the zero/default legacy path. DRAM-clock-accurate timing, refresh energy, bank groups, and queue-aware refresh overlap remain unmodeled. |
| P0 | Deferred-refresh cadence: nominal-grid debt / reset-after-service / credit-counted adaptive | `dram-deferred-refresh-debt.md`; late refresh must consume bounded slack without silently lowering the long-term refresh rate | Nine live rows prove the next nominal due time stays at 2×tREFI regardless of first-fire delay; later slack falls to 5,001 cycles at max-deferral-minus-one. Debt preservation protects retention cadence but can relocate latency spikes | Live `refresh_next`, event deadlines/counts, returned service, fail-closed sweep gates | **DONE audit/gate 2026-08-05.** Nominal-grid behavior was already implemented and is now protected by a 9-row sweep; false reset-after-service prose was corrected. Reset-after-service is **EXCLUDED** as retention-unsafe without credits. Queue-aware credit-counted postpone/pull-in is **BLOCKED** on request queues, arbitration, trace evidence, and an explicit JEDEC credit contract. |
| P2 | Weight stream: explicit bitmap / adaptive raw-RLE-bitmap | `bitmap-weight-compression.md`; scattered zeros make RLE expand while a fixed one-bit occupancy map has placement-independent metadata | Random 10–90% zero payload cycles fall 3.5–82.8% vs raw and 66.7–76.3% vs RLE; costs bitmap scanner/value merge, variable-rate buffering, format state, and greater verification | Exact bytes, payload cycles, selected codec, bit-exact round-trip, malformed-stream rejection, config parse | **DONE 2026-07-21.** Runtime BITMAP and three-way ADAPTIVE modes, portable format, frame/DMA integration, 21 focused tests, 12-row sweep. Default NONE. Decoder throughput/area/power unmodeled. |
| P3 | Compression alternatives beyond bitmap/RLE (block bitmap, entropy) | Bitmap closes the evidenced random-zero gap; block formats may reduce bitmap scan/alignment cost, while entropy coding is substantially more complex | Potential metadata/decode trade-offs, but no current evidence that another format adds decision value beyond raw/RLE/bitmap | No implementation or sweep | **BLOCKED pending a concrete hypothesis.** Do not add codec proliferation until decoder modeling or compiler traces expose a gap. Entropy coding excluded for now because throughput/area cannot be validated. |
| P1 | Dense reconstruction / direct compressed-domain sparse-compute feed | Decoder sweep proves dense W-buffer reconstruction needs >DMA-width output to improve load latency; real sparse accelerators may consume packed weights directly | Direct feed can avoid zero writes and dense expansion, but couples codec, sparse MMA, FIFO/backpressure, ISA, and compiler packing; sacrifices modularity and greatly increases control/verification burden | Compression stream metadata and standalone 2:4 MMA cycles exist, but no shared execution path or compatible encoding | **BLOCKED on an architecture contract.** Define codec-to-MMA token format, supported sparsity semantics, FIFO model, fallback behavior, and differential verification before implementation. Do not claim dense-reconstruction results for this architecture. |

## Audit notes

- The 2026-09-02 audit found no READY backlog item, so it tested the next bounded hypothesis exposed by the executable DMA command-rate model: load and store command pipelines may have unequal visible issue costs even when bus width and burst limits match. Symmetric, read-overlapped, and write-overlapped modes are all preserved; neither asymmetric row is selected universally.
- In the gated 96-byte, three-burst matrix, explicit 0/4-cycle read/write issue lowers load completion from 60 to 54 cycles while raising store completion to 66; 4/0 reverses the exchange. Useful bytes, burst count, payload serialization, and numerical behavior are unchanged. Expected queue/credit area, command/control energy, shared-memory throughput, and calibration remain unquantified.
- Directional issue fields carry explicit presence metadata because zero is a meaningful fully-overlapped setting while absent fields must inherit the common compatibility cost. Existing APIs and zero-initialized runtime callers continue to inherit; JSON can select an explicit zero independently in either direction.
- The 2026-09-01 audit found no READY backlog item, so it tested a concrete follow-up hypothesis from the executable DMA command-rate model: read and write paths may have different buffering, masking, and protocol limits. Symmetric, read-wide, and write-wide modes are all preserved; no asymmetric row is selected universally.
- In the gated 96-byte matrix, 128/32-byte read/write limits exchange two cycles of load latency for two cycles of store latency relative to symmetric 64/64, while 32/128 reverses the result. Useful bytes, payload serialization, and numerical behavior are unchanged. Expected buffer/credit area, command energy, masks/coalescing, shared-memory throughput, and calibration remain unquantified.
- Directional fields use zero as inheritance rather than redefining the historical common maximum. Existing initializers and memset-zeroed runtime callers therefore retain symmetric behavior; explicit directional values propagate through JSON/YAML/generator/canonical/runtime/live-engine paths and fail closed when invalid.
- The 2026-08-31 audit found that `dma_max_burst_bytes` was a documented no-op and that assigning it timing significance without a separate command-rate contract would be ambiguous. The implementation therefore preserves max-burst size and visible per-burst issue cost as orthogonal runtime settings. Zero issue cost remains a materially useful fully-overlapped/legacy lower bound rather than being replaced by the locally lowest nonzero-cost row.
- In the isolated 4 KiB/256-bit matrix, one visible issue cycle makes 32/64/128-byte bursts complete in 307/243/211 cycles; larger bursts reduce abstract command count but do not change the 128 payload cycles or 4,096 useful bytes. Expected buffer/credit area, control energy, alignment constraints, head-of-line blocking, and physical timing are unquantified, so this is command-rate sensitivity rather than a universal large-burst recommendation.
- The 2026-08-30 audit followed the existing memory-latency configuration into the live descriptor engine. Separate read/write values and the already documented core-cycle/physical-ns domain were canonical-only; descriptor stores and queued projections always used the read compile-time constant. The new path preserves symmetric, read-fast, and write-fast alternatives without selecting one universally.
- In the isolated 4 KiB/256-bit matrix, 30/70 cycles lowers load completion 11.2% versus 50/50 while raising store completion 11.2%; 70/30 reverses the exchange. Byte traffic and numerical behavior are unchanged. Area, controller/PHY energy, queue throughput, shared contention, and end-to-end compute overlap are unquantified, so these are directional service points rather than a throughput recommendation.
- The 2026-08-29 audit revisited a historically explored architecture knob instead of adding a new feature family. `dma_bus_width_bits` was executable only in parsing/validation and generated macros; canonical-to-runtime conversion omitted it and the live descriptor engine always used 256 bits. The live 4 KiB sweep now preserves 128/256/512-bit alternatives and exact 307/179/115-cycle completions without calling the widest mode universally best.
- The measured width benefit buys expected datapath, FIFO, SRAM-port, NoC/DRAM interface, routing, timing, power, and verification cost that the cmodel cannot quantify. Byte traffic and numerical accuracy are unchanged. The result is restricted to isolated base-plus-serialization descriptor service with SRAM metering disabled; compile-time analytical engine surfaces are still explicitly separate.
- The 2026-08-28 follow-up adds `least_projected_cycles` without replacing count- or byte-based alternatives. At cycle 307 it observes 128 live cycles remaining on the 12 KiB active transfer, places the final 4 KiB descriptor behind it, and matches the 613-cycle batch instead of least-bytes' 664 cycles. This result is restricted to the measured independent-path, SRAM-meter-disabled regime. Queued estimates include only base latency and payload serialization; stateful SRAM stalls, contention, heterogeneous rates, and physical controller cost remain unquantified.
- Projected-cycle binding is plausible when hardware already carries completion countdowns, but it requires timestamp subtraction plus queue accumulation and a stable cycle contract. Least-bytes remains plausible for simpler variable-length accounting and early arrivals; least-outstanding minimizes state width; round-robin minimizes comparison logic; explicit preserves semantic W/A/O affinity and compiler control. No policy is selected universally.
- The 2026-08-27 follow-up adds `least_bytes` without replacing descriptor-count binding. It exposes a useful model boundary: assigned bytes are more balanced in the measured staged workload, but batch completion is 8.3% later than round-robin/least-outstanding because the live engine has no partial-progress byte state and charges a nearly completed 12 KiB active descriptor at full size. A hardware team may still choose byte totals for variable-size early arrivals, accepting wider accounting/comparison logic; remaining-service prediction is deferred until progress is executable.
- The 2026-08-26 audit followed the live DMA producer-to-queue boundary rather than adding another service policy. Explicit, round-robin, and least-outstanding binding are preserved because dedicated semantic channels, low-cost homogeneous distribution, and dynamic queue-aware systems are all plausible. Automatic binding is not called universally better: its measured 53.7% batch reduction requires intentionally skewed producer placement plus independently serviceable paths; a shared-serial bus has no added payload capacity, and explicit placement retains compiler affinity/dependency control.
- Least-outstanding counts descriptors, not bytes or predicted remaining time. In the staged unequal-size workload it sends the first two second-wave descriptors to idle channels (486/486 cycles versus round-robin 613/486) but sends the third behind the long transfer (613 versus 486), leaving batch completion tied at 613. Byte traffic and arithmetic are unchanged; comparator area, switching energy, physical path replication, and shared SRAM/DRAM effects remain unquantified.
- The 2026-08-25 audit used the shared-serial mover's live simultaneous-ready contract to compare fair rotation with strict descriptor priority. Strict priority is not called faster: it preserves the same 6,487-tick batch completion while exchanging low-priority latency for a 50.0% lower critical completion time in the measured 0/10/5 priority batch. Round-robin remains the default; strict priority remains optional because starvation, software annotation, comparator cost, and verification burden are real sacrifices.
- Strict priority is descriptor-boundary and non-preemptive. Equal priority retains the rotating tie-break. Aging, weighted policies, beat-level preemption, and automatic compiler/runtime assignment remain blocked on sustained-arrival traces and explicit QoS/replay contracts rather than being inferred from this finite batch.
- The 2026-08-24 follow-up corrected the strongest realism caveat in the channel-count matrix: multiple scheduling queues no longer have to imply multiple full-bandwidth physical data paths. Independent paths preserve historical behavior and expose concurrency; shared-serial preserves stream queues while allowing one active payload transfer globally. No universal topology is selected because the measured latency gains buy unquantified engine/port/routing/power cost.
- Shared mode uses non-preemptive descriptor-boundary arbitration with round-robin and strict-priority alternatives. This is not evidence for beat-level arbitration, preemption, a shared DRAM makespan, or queue-aware GEMM overlap. Partially shared fabrics remain blocked on aggregate-bandwidth, replay, and memory-port contracts.
- The 2026-08-23 DMA re-audit replaced an analytical-only recommendation with live descriptor-engine evidence. Canonical `channels`, `max_outstanding`, and `async_mode` were parsed and documented but dropped by canonical-to-runtime conversion, while `tu_init_with_config()` always used compile-time defaults. They now propagate to the executing engine. The sweep preserves one-, two-, and three-channel candidates: one minimizes engines/control, two fully serves two independent streams, and three helps only when a third stream is simultaneously ready. Queue depth is reported as admission capacity, not a throughput speedup, because producer issue latency is absent.
- The same audit corrected two model-safety defects: a selected count above the compiled array capacity was silently clamped to eight even though the array had only `TU_DMA_CHANNELS` entries, and `max_outstanding` ignored an active descriptor after dequeue. The engine now has an explicit eight-channel model capacity, fails closed above it, and counts active plus pending work. Counts four through eight are supported model capacity for general channels, not an evidence-backed recommendation for W/A/O GEMM.
- The live 4 KiB rows include 1,984 SRAM-stall cycles from the current refill meter, so their 2,163-tick single-stream completion is not a physical AXI/DRAM calibration. Shared-port contention, request replay, queue producer timing, host submission overhead, automatic priority assignment, binding flexibility, and end-to-end compute overlap remain unmodeled; no area, power, or energy numbers are invented.
- The ready candidates were re-audited against current code and executable evidence. With no READY backlog item, the 2026-08-22 audit revisited SRAM port assumptions and found an executable configuration defect: canonical bank provisioning was not propagated, and a one-word budget refilled every four cycles had been mislabeled as one word/cycle. Grant and cadence are now executable runtime dimensions; the old 1/4 behavior remains default.
- The SRAM re-audit also invalidated historical arbitration conclusions as live evidence. `arb_mode` is metadata only, the analytical source harness is absent, and scalar requests cannot express simultaneous contenders. Arbitration remains blocked rather than assigning invented RR/priority behavior.
- No realistic alternative was removed because it lost a local benchmark. NONE remains the backward-compatible compression default despite RLE's large gains on clustered data.
- `docs/exploration/TERMS.md` remains pre-existing untracked user work and was not modified or included in this heartbeat.
- Adaptive compression preserves NONE and explicit RLE as materially useful alternatives: fixed-function ASICs can avoid codec cost, controlled software stacks can omit the frame, and heterogeneous runtimes can choose bounded per-tensor fallback.
- Context retention alternatives are preserved rather than selecting the lowest-latency row. Full save supports transparent preemption; live-prefix save supports compiler/runtime liveness contracts; control-only supports low-area hardware that reloads scratchpads.
- Baseline infrastructure issue fixed: `test-asm` no longer depends on undocumented `/tmp` files, so a clean `make test-quick` is reproducible.
- The structured-sparsity audit corrected a stale existence claim: the module was tracked but omitted from `TU_OBJS` and all test aggregates. It is now executable through focused targets. Dense remains the default because it avoids pruning error and sparse-control cost.
- Decoder provisioning is now explicit rather than assuming infinite decode bandwidth. The measured narrow-N row demonstrates why 2:4 cannot be summarized as an unconditional 2x speedup.
- The bitmap audit compared identical random and clustered patterns rather than treating scalar sparsity as sufficient evidence. Bitmap, RLE, raw, two-way adaptive, and three-way adaptive modes are all retained because their hardware and workload regimes differ.
- The three-way selector minimizes payload bytes, not end-to-end latency. Decoder throughput, FIFO stalls, area, and power remain explicitly unquantified; no universal codec recommendation is made.
- Decoder throughput is now executable rather than assumed infinite. The common dense-output lane plus codec metadata-rate abstraction covers materially distinct low-area through extra-wide hardware and streaming versus staged placement without inventing physical area/power numbers.
- The decoder sweep rejected the previous payload-only speedup implication: 1–8 output/cycle decoders are 2–16× slower than raw in this workload, 16/cycle generally breaks even, and 32/cycle yields up to 2× lower load latency only in measured sparse regimes. Traffic savings remain valuable even when latency does not improve.
- A new direct compressed-domain compute candidate is intentionally blocked: current RLE/bitmap streams reconstruct dense SRAM and cannot honestly stand in for sparse token delivery to MMA.
- The interconnect audit separated switching from topology. The prior topology sweep had silently assumed store-and-forward and multiplied full payload serialization by hop count; its universal MESH recommendation is now explicitly superseded by traffic-matrix results.
- Cut-through, store-and-forward, and the legacy timing lower bound are all preserved. Cut-through wins measured long-route tensor transfers but adds VC/credit/deadlock complexity; store-forward remains plausible for simple fabrics and bridge boundaries.
- Focused static multicore verification exposed two latent defects: an FP16 interpretation of the FP32 O-buffer and dataflow registry replacement that could invalidate prior core pointers. Both remain covered by the multicore suite.
- The 2026-07-24 contention audit implements ideal-parallel and shared-directed-link lower bounds rather than selecting one fidelity level. At 16 cores the MESH is 55.4% lower for all-to-all but 48.6% slower for hotspot fan-in under deterministic XY; topology selection is workload- and routing-dependent.
- Shared-link accounting is explicitly not a queue simulator. Finite FIFO depth, injection timing, arbitration, head-of-line blocking, adaptive routing, collective algorithms, and router area/power remain unmodeled rather than assigned invented costs.
- The 2026-07-25 route-order audit preserves deterministic XY and YX rather than retaining only the locally faster row. YX cuts the 4×4 top-row-to-left-column lower bound from 2,334 to 798 cycles, while XY gives the same gain for the transposed workload; symmetric all-to-all is unchanged.
- Fixed YX closes the evidenced deterministic route-order gap without pretending to be adaptive routing. Queue-aware adaptive routing remains blocked on injection, arbitration, virtual-channel, backpressure, and deadlock contracts grounded in compiler or collective traces.
- The 2026-07-26 dataflow re-audit invalidates the historical WS/OS/RS ranking as comparative evidence. It proves direct-global selection, canonical arithmetic, edge geometry, K-tile callback counts, and runtime pipeline depth, but labels timing only as an uncalibrated deterministic estimate. OS is lowest at depth 2 in the measured matrix while RS is lowest at depth 1 because of OS's ad hoc fetch term; no universal mode is selected.
- The 2026-07-27 per-core dataflow study closes the selection/isolation contract: WS, OS, and RS survive independent core swaps, produce byte-identical outputs, retain distinct live cycle deltas, and reject NLR without fallback. This enables homogeneous versus heterogeneous mapping studies but does not model switch cost or parallel execution.
- Dataflow traffic observability remains blocked: define RF/SRAM/NoC event boundaries and overlap before adding bandwidth, energy, or physical-stationarity claims. Per-core plugin-private statistics are also blocked on ownership/snapshot semantics because registry objects are process-shared.
- The 2026-07-28 audit found a configuration-provenance defect in the existing power model: process and clock were silently inferred from array dimensions and DRAM bandwidth despite YAML advertising a power block. AUTO plus all six explicit process presets are retained; no node is selected as universally best because mask/NRE, yield, reliability, physical timing, and supply risk are absent from the scalar tables.
- Explicit clock selection is intentionally labeled a constant-voltage timing assumption, not DVFS. At fixed 7nm event counts, higher clock lowers modeled leakage duration but raises average power; voltage-dependent `CV^2`, feasible F-V points, transition cost, thermal response, and delivery constraints remain a BLOCKED future contract.
- Focused verification exposed stale shared-library selection in the historical `test-power` target. It now links `./libtucmodel.a` explicitly, so canonical-struct changes cannot be tested against an old `.so`.
- The 2026-07-29 DRAM audit found that `model_row_conflicts=true` charged a constant read-only penalty without tracking rows. Explicit open/closed policies now model read and write locality while legacy remains the zero/default compatibility path. Open page wins only on the measured locality-bearing patterns; row thrash erases the gain, so neither policy is universal.
- DRAM row-policy results use returned service cycles only. The module's bandwidth/channel stall accounting is a separate, known coarse domain and was not added to the policy table. Physical tRCD/tRP/tRAS, bank groups, refresh, queues, reordering, and activate energy remain unquantified.
- The 2026-08-07 row-timing follow-up preserves the historical equal-cost behavior through `row_conflict_penalty_cycles=0` and the old public setter, while exposing empty activations and open-row replacements separately. In the 20/40-cycle matrix, sequential and bank-stream locality still favor open-page, but alternating rows favor closed-page; neither policy is selected universally.
- The 2026-07-31 address-mapping follow-up adds a bounded XOR-interleaved alternative after a discriminating synthetic stride exposed a concrete alias. XOR preserves burst mode's bank/row identity and service sum while balancing the 2 KiB-stride accesses across four channels; it is rejected for non-power-of-two channel geometries.
- The 2026-08-04 refresh audit adds JEDEC tREFI/tRFC accounting behind the existing channel/bank/row model. All-bank, per-bank, fixed, deferred, and 1x/2x/4x are runtime alternatives because real controllers differ by complexity budget and retention requirement; NONE remains the zero/default compatibility path. The burst-idle sweep demonstrates the scheduling reversal (fixed pays 0 refresh stall, deferred +325%) that prevents any universal recommendation, matching the deferred-refresh hazard of firing at the first post-schedule access.
- Refresh lockout is a service cost: it flows into returned cycles and a dedicated `total_refresh_stall_cycles` counter, deliberately outside `total_stall_cycles` (same convention as row penalties). The counter sums per-access remainders, so a dense stream legitimately exceeds events×tRFC: with ≤1 access/cycle the correct sweep bound is events×dur(dur+1)/2, not events×dur (the sweep's initial gate was corrected accordingly). Refresh energy, bank groups, DRAM-clock-accurate conversion, and queue-aware refresh overlap remain unmodeled.
- The refresh phase-alignment sweep (11 phases across tREFI) converted the two-point fixed/deferred anecdote into a continuum. Fixed is a phase sawtooth with zero stall on most phases; deferred is phase-insensitive and strictly worse for this workload whose idle gaps are shorter than tREFI, because a post-schedule access always triggers the outstanding refresh. Long idle can let either fixed or deadline-fired deferred refresh complete without charging an access; a deferred throughput benefit is not claimed without queues.
- The 2026-08-05 debt sweep found a claim-bearing documentation error: the implementation retains the nominal tREFI grid after a late command, while the exploration text incorrectly described reset-after-service. Nine delayed-fire rows prove `next_grid=2×tREFI`; reset-after-service is excluded because repeated late commands could under-refresh. A credit-counted adaptive controller remains blocked on queue/arbitration/trace and JEDEC-credit contracts.
- The 2026-08-08 clock-domain sweep preserves fixed physical DRAM bandwidth and refresh ns across 0.5/1/2 GHz by converting to bytes/core-cycle and TU cycles. It does not claim a universal clock choice: higher clock lowers bytes/core-cycle and carries unmodeled voltage, timing-closure, clock-tree, thermal, CDC, and verification costs. The fixed read/write latency cycle term remains an explicit calibration gap.
- The 2026-08-09 latency-domain sweep makes that aggregate term explicit rather than silently choosing one interpretation. Fixed core cycles preserve old tables and fixed-stage abstractions; physical ns preserves a fixed external-time assumption. The 4 KiB estimate stays exactly 114 ns across 0.5/1/2 GHz only in physical-ns mode. Neither mode decomposes or calibrates controller, CDC, PHY, and DRAM-command latency.
- The 2026-08-10 adaptive row-policy sweep preserves both static endpoints and adds a per-bank timeout rather than replacing either. It matches open-page under short reuse, closed-page after long idle, remains vulnerable to immediate row thrash, and wins only the measured burst-with-idle regime. Timeout precharge is lazy and command/energy costs are unquantified, so this is row-state service evidence rather than controller throughput evidence.
- The 2026-08-11 timeout-domain sweep preserves fixed core cycles rather than replacing them with the locally more stable physical-ns interpretation. Fixed cycles are a simpler synchronous-controller contract but vary from 16 to 4 ns across 0.5 to 2 GHz; physical ns preserves the 8 ns policy through 4/8/16-cycle conversion. Dynamic clock transition, separate DRAM-clock, CDC, command legality, and energy remain explicit gaps.
- The 2026-08-12 bus-turnaround sweep preserves NONE as the compatibility/independent-path lower bound and adds symmetric/asymmetric fixed timing rather than retaining only the locally lowest row. Unidirectional traffic is unchanged; alternating traffic pays every transition, and batching pays one direction-dependent boundary. This is invocation-order service accounting, not an automatic write-drain scheduler; queues, dependency/fairness policy, command/data-bus overlap, PHY energy, and calibration remain blocked.
- The 2026-08-13 idle-credit follow-up closes a temporal overcharge without replacing the conservative mode. Back-to-back requests retain the full 3/8-cycle costs, partial idle subtracts only elapsed time after prior service completion, and sufficient idle reduces residual cost to zero. It remains a coarse channel-availability contract, not evidence of JEDEC command/data-burst completion timing.
- The 2026-08-14 completion-boundary follow-up exposes two useful abstractions rather than silently redefining idle credit. Base-service credit is simple and optimistic about payload overlap; burst credit delays availability by `ceil(bytes/bus_width)` and is conservative for a serialized shared bus. It still does not model command/data overlap, burst length protocol, queues, or out-of-order completion.
- The 2026-08-15 burst-granularity follow-up closes the specific minimum-transfer omission while preserving exact-byte occupancy for byte-enable/coalesced links. Protocol rounding changes only sub-burst and tail requests: aligned 64 B traffic is identical, while 16 B and 80 B cases retain turnaround longer. The 2026-08-16 follow-up makes that same occupancy visible in separate counters and the coarse bandwidth budget: measured pair traffic rises 4.0× at 16 B and 1.6× at 80 B, with no aligned penalty. The 2026-08-17 follow-up reports both useful and occupied bandwidth: rounded payload efficiency is 25.0% at 16 B, 100.0% at aligned 64 B, and 62.5% at 80 B. The 2026-08-18 follow-up splits the granule by direction: 128/32 B and 32/128 B contracts reverse which transition retains idle-credit cost. The 2026-08-19 follow-up distinguishes payload-size rounding from address-span rounding: a 64 B request at address 1 occupies one versus two 64 B bursts, while an 80 B request at address 63 occupies two versus three. Exact-byte controls remain available. These remain deterministic observation metrics, not PHY energy or queue-aware throughput.
- Mapping channel occupancy is exact decode evidence, not throughput. Queue-aware overlap, arbitration, bank-group timing, more complex hashing, and activate/decode energy remain blocked, so the sweep does not convert channel balance into makespan or energy. Predictable burst/row modes remain useful for explicit software placement despite XOR's local balance result.
- The zero-byte matrix preserves every timing mode for nonempty requests rather than selecting a locally cheapest architecture. Minimum-burst charging for an empty span is excluded as semantically unsupported; command-level rejection is deferred until a producer/consumer surface has executable status, signaling, and retirement contracts. The planning estimator now shares only the zero-work contract; for nonempty traffic it remains intentionally coarser than address-aware service.
- Remaining open candidates are blocked on architecture contracts, traces, or physical-model dependencies; no other verified READY item was found in the 2026-09-02 audit.
